using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UISystem : MonoBehaviour
{
    // Invoke event StartFire
    
    // Make script burn (repeating decrease of hp)
    
    // Throw event Dead when it is lower than 0
}
